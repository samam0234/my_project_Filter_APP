# Docker runtime (Phase 1 API + OpenCV + DB)
# Heavy ML (ultralytics/torch) is optional — segmentor falls back to stub mask.

fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic==2.9.2
pydantic-settings==2.5.2

opencv-python-headless==4.10.0.84
numpy==1.26.4
Pillow==10.4.0

# optional ONNX (lighter than full torch stack)
onnxruntime==1.19.2

python-dotenv==1.0.1
aiofiles==24.1.0
loguru==0.7.2

SQLAlchemy==2.0.35
PyMySQL==1.1.1
cryptography==43.0.1

# LangGraph stack is large; install minimal stubs path without full langchain
# Uncomment for full LLM workflow in container:
# langchain==0.3.1
# langchain-core==0.3.6
# langgraph==0.2.28
# langchain-openai==0.2.1
