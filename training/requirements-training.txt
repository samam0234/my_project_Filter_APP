# 학습 전용 (루트 requirements.txt 와 분리)
# Python 3.11~3.12 권장
#
# ========== CUDA (이 PC) ==========
# 시스템 설치본 사용 — 추가 CUDA Toolkit 다운로드 하지 말 것
#   경로 예: C:\Program Files\NVIDIA GPU Computing Toolkit\CUDA\v13.3
#   셸:  . .\env_cuda.ps1
#   venv: .\setup_cuda_env.ps1   (torch 원격 자동 설치 없음)
#
# torch / torchvision:
#   - requirements 에 넣지 않음 (pip 가 2GB+ 휠을 몰래 받지 않게)
#   - 로컬 wheel 또는 사용자가 지정한 소스만 설치
#   - 예: pip install D:\wheels\torch-xxx.whl
# =================================
#
# 경량 패키지만 여기 나열. ultralytics 는 torch 설치 후 따로.

numpy>=1.26.0,<2.3
Pillow>=10.4.0
PyYAML>=6.0
tqdm>=4.66.0
matplotlib>=3.8.0
opencv-python-headless>=4.10.0

# torch 설치한 뒤:
# ultralytics>=8.3.0

# LoRA / Phase 2 (학습할 때만. --dry-run 은 여기 없이 동작)
# peft>=0.12.0
# transformers>=4.44.0
# accelerate>=0.33.0
# datasets>=2.20.0
#
# Ollama GGUF 는 PEFT 에 쓰지 말 것. --base-model 은 로컬 HF 디렉터리.
