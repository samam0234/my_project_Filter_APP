# Training: SQLite + MariaDB

## 포인트

- 동일 ORM 모델로 dialect 전환
- `JSON` 타입, FK CASCADE 동작 차이 주의
- 로컬 파일 DB vs 서버 DB URL 구성
- `create_all` vs Alembic (마이그레이션 학습 과제)

## 설정 키

`DB_DIALECT`, `SQLITE_PATH`, `MARIADB_*`, `DATABASE_URL`
