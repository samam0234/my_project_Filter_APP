# 컷앤킵 — Docker 런타임용 경량 Python 의존성 (저장소 루트)
# Phase 1 API + OpenCV + DB
#
# 무거운 ML(ultralytics/torch)은 기본 미포함.
# → Segmentor 는 모델 없으면 stub 마스크로 동작.
#
# 로컬 풀스택 개발: requirements.txt
# 학습: training/requirements-training.txt

fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic==2.9.2
pydantic-settings==2.5.2

opencv-python-headless==4.10.0.84
numpy==1.26.4
Pillow==10.4.0

# optional ONNX (full torch 스택보다 가벼움)
onnxruntime==1.19.2

python-dotenv==1.0.1
aiofiles==24.1.0
loguru==0.7.2

SQLAlchemy==2.0.35
PyMySQL==1.1.1
cryptography==43.0.1

# LangGraph 스택은 용량이 큼. 컨테이너에서 전체 워크플로 필요 시 주석 해제:
# langchain==0.3.1
# langchain-core==0.3.6
# langgraph==0.2.28
# langchain-openai==0.2.1
